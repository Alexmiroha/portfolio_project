let initialState = {
    LoginData: [],
    }

const loginReduser = (state = initialState, action) => {

    switch (action.type) {
               default:
            return state;

    }
}

export default loginReduser;