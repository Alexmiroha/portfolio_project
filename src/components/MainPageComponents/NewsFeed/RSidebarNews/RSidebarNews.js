import React from 'react';
import s from './RSidebarNews.module.css';

const RSidebarNews = (props) => {
    return (
        <div className={s.RSidebarBlock}>
            RSidebar
        </div>
    );
};

export default RSidebarNews;