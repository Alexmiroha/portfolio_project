import React from 'react';
import s from './Login.module.css';

const Login = (props) => {

    return (
        <div>
            <h1 className={s.test}>Login</h1>
        </div>
    )
};

export default Login;
